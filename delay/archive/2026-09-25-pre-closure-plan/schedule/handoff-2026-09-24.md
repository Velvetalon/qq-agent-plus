# qq-agent-plus vNext 交接状态

更新时间：2026-09-24（Asia/Shanghai）  
工作区：`G:\UGIT\qq-agent-plus`  
分支：`main`  
基线 HEAD：`e2abe9416afe154bd495b5190e66abbcc06e41b8`  
协调任务检查点：`C:\Users\v_whcnwwang\.codex\task-checkpoints\tasks\20260924T093253Z-ad40\checkpoint.md`

## 当前阶段

当前正在执行 **P2：生命周期和三个扩展点真正接通**。P0、P1 已完成并由协调者复核，尚未进入 P3。

P2 任务卡：[`delay/task/P2-plugin-lifecycle.md`](../task/P2-plugin-lifecycle.md)  
完整规划：[`plan_doc/2026-09-24-little-w-vnext-development-plan.md`](../../plan_doc/2026-09-24-little-w-vnext-development-plan.md)

## 已完成并可复核

### P0 基线

- 产物：[`docs/verification/vnext-baseline.md`](../../docs/verification/vnext-baseline.md)
- 证据目录：`docs/verification/p0-20260924/`
- HEAD 与规划基线一致；部署 revision 一致；服务 `qq-agent-linux` 当时为 active。
- Node 24 下 focused prompt/local/scroll/usage 测试通过。
- unit/render 存在已记录的基线 FAIL；Node 16 默认环境标记为 BLOCKED；没有把这些问题误记为 PASS。
- 未读取或保存生产私聊、凭据、token、API key 或隐藏推理。

### P1 Registry + Legacy Adapter

- 产物：[`docs/verification/vnext-p1-plugin-registry.md`](../../docs/verification/vnext-p1-plugin-registry.md)
- 新增 `src/plugins/contract.js`、`registry.js`、`manager.js`、`context.js`、`builtin/legacy-tools.js`。
- Orchestrator 已接入每个 Run 的 plugin snapshot；`tools.js` 仍通过旧 `tools-core.js` 执行。
- Legacy 23 个工具的名称、顺序、OpenAI schema 与基线深度一致；Registry focused 4/4，Orchestrator 30/30，Tools 5/5，Scheduler 13/13，多模态 11/11，prompt/local/scroll/usage focused 均通过。
- P0 的全量 unit/render 失败作为 carry-forward 保留，没有发现 P1 新失败组。

## 当前活动执行者

- 执行者：`gpt-6-luna`
- 协作任务：`/root/p0_baseline`（该名称沿用，当前已被 follow-up 用于 P2）
- 当前范围：只实现 P2；负责 `Orchestrator`、`ChatStore`、插件生命周期/扩展点、测试与验证文档。
- 不得提前实现：`stay_silent`、Notebook、检索、Reflection、Learned Self、P3 之后的 UI/发布工作。
- 当前 Git 工作区中 P2 已有未提交变更，涉及：
  - `src/console/app.js`
  - `src/core/orchestrator.js`
  - `src/core/store.js`
  - `src/tools/tools.js`
  - `test/plugin-lifecycle.test.mjs`
  - 以及 P1 的 `test/plugin-registry.test.mjs` 和 `src/plugins/`

## P2 必须保持的契约

- 插件只支持随源码发布、管理员审查的可信内置 JavaScript；不做动态安装、第三方沙箱或 LLM 生成代码执行。
- 三类扩展点：Tool、ContextProvider、SessionObserver。provider 失败必须降级，不得拖垮聊天；observer 至少一次投递且幂等。
- 未启用插件不得创建自有 DB、timer、后台模型请求或上下文注入。
- 停用顺序：阻止新调用 → generation 失效/取消 → 清理 timer → 中止请求 → 有限 drain → 释放资源；晚到写入不得提交。
- 完成事件必须在现有 ack/complete 或 lifecycle 提交事务中由宿主写入；不得重放已 ack 的 QQ 入站消息。
- 不绕过现有 Sender/Outbox、lease/ack、lifecycle 原子提交、`tools.js` 调度包装、多模态结果配对。
- 迁出 runtime-control、messaging、memory-tools 后，Legacy 清单必须扣除对应工具，不能出现双 owner；其余工具暂留 Legacy。

## 交接后的下一步

1. 先等待 P2 执行者报告，不要在同一共享文件上另起并行实现。
2. 检查 P2 diff 是否越界，重点审阅 `src/core/orchestrator.js`、`src/core/store.js`、`src/console/app.js`、`src/tools/tools.js`。
3. 运行 P2 任务卡要求的 focused lifecycle/provider/observer/recovery 测试，并复核 P0/P1 回归。
4. 只有 P2 合并门槛满足后，才派发 P3；P3 才能实现 `stay_silent` 和参与规则整治。
5. 若当前 Luna 连续三次无法拉起，按用户授权回退到 `deepseek-flash`；不要因此跳过 P2 验收。

## 当前工作区与安全说明

- 用户先前创建的 `AGENTS.md`、`plan_doc/`、`delay/task/` 均为有意变更，不得回退。
- `docs/verification/` 是本轮验证证据；不得删除失败日志来制造绿色基线。
- 当前没有执行生产部署、生产外发、凭据配置或数据库破坏性迁移。
- 继续工作前先执行 `git status --short --branch`，确认共享工作区实际状态；不要依据本交接文档猜测未提交代码的最终内容。
