baseline (8bade7f) failing tests: 46
p7 worktree failing tests:    46
compare-object diff (empty = identical sets):
  <none>

failing test names (both):
a lone .env never implies a managed installation
a managed stack may be checked repeatedly with all of its own ports occupied
a managed stack with a stopped container and Agent can be updated
a not-found service reported with nonzero exit still permits fresh installation
approved friend proposal dispatches once and waits for friend_add confirmation
configure-linux creates observe config and preserves runtime mode on update
disabled existing pilot remains readable but cannot mutate or capture
fresh preflight succeeds without creating the stack or asking for model credentials
friend proposal validation enforces message threshold and administrator configuration
friend proposals require eligibility, deduplicate, cool down, and close on friend_add
identity pilot is promoted: legacy enabled:false no longer gates it
incident pilot quarantines an unknown write without blocking later messages
incoming friend request keeps an unknown result and never retries automatically
incoming friend requests notify once, require approval, and add the private whitelist
installed manage launcher uses the exact deployed Node runtime
keeps explicit OneBot business rejection retryable without holding the chat
leftover QQ login data is protected even without any Agent configuration
legacy production state is refused before credentials, dependencies or services change
managed preflight refuses mismatched changed live Token without writing
managed preflight refuses mismatched Compose volume source without writing
managed preflight refuses mismatched container project without writing
managed preflight refuses mismatched container volumes without writing
managed preflight refuses mismatched corrupt configuration without writing
managed preflight refuses mismatched production OneBot endpoints without writing
managed preflight refuses mismatched service directory without writing
managed preflight refuses mismatched unrelated Compose services without writing
manual incident control blocks model work while preserving unread input
no published release means the updater never fetches or deploys
ordinary managed update does not attempt password rotation and preserves Agent configuration
persists mode, starts headless and exits cleanly on SIGTERM
pre-created empty data directories do not count as an existing installation
preflight protects external SnowLuma at another root
preflight protects failed Docker inventory
preflight protects failed service inspection
preflight protects inaccessible Docker daemon
preflight protects Nginx on the selected WebUI port
preflight protects pre-existing Agent service
preflight protects unavailable port inventory
probe request checks repository and branch without fetching or deploying
protects console APIs, redacts credentials and blocks all sending in observe mode
scheduled updater uses the persistent Git cache and records no-update
test\\identity-pilot-nodiff.test.mjs
transient GitHub TLS failures are retried before fetch
unifies the same QQ across chats and indexes legacy memory without modifying it
unknown friend request result is held and a crashed dispatch is recovered as unknown
updater tests a checkout and delegates deployment with the exact revision
